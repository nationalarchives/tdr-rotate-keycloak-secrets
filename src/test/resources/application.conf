keycloak {
  url = "http://localhost:8000"
  user {
    admin {
      client = "test-client"
      secret_path = "/a/secret/path"
    }
  }
}
environment = "test"
sns {
  endpoint = "test"
  topic = "arn:aws:sns:region:account:name"
}
ssm {
  endpoint = "http://localhost:8080"
}
